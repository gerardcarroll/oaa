<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.overstock.android.checkout.impl">

  <application
    android:label="@string/app_name"
    android:supportsRtl="false">
    <activity
      android:name="com.overstock.android.checkout.ui.CheckoutActivity"
      android:label="@string/checkout_title"
      android:launchMode="singleTop"
      android:theme="@style/Theme.Overstock.Transaction.Checkout" />
    <activity
      android:name="com.overstock.android.orderconf.ui.OrderConfirmationActivity"
      android:label="@string/order_complete"
      android:theme="@style/Theme.Overstock.Action.OrderComplete"/>
  </application>

</manifest>